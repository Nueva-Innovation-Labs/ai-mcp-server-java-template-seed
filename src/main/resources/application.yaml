spring:
  application:
    name: ai-mcp-server-java-template-seed
  # -------------------------------------------------------------------
  # Database Configuration (PostgreSQL)
  # -------------------------------------------------------------------
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_database
    username: n8nadmin
    password: m1ni0n1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update # Automatically update the schema based on entities
    show-sql: true # Log SQL statements
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  ai:
    mcp:
      server:
        name: contact-information
        version: 0.0.1
        protocol: STREAMABLE
# -------------------------------------------------------------------
# Spring AI / MCP Server Configuration (optional but good practice)
# -------------------------------------------------------------------
server:
  port: 8085